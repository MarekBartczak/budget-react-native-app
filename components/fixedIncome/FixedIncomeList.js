import { StyleSheet, Text, View } from "react-native";
import React from "react";
const FixedIncomeList = (props) => {
  return (
    <View>
      <Text> list stalych wplywow</Text>
    </View>
  );
};

export default FixedIncomeList;

const styles = StyleSheet.create({});
