import { StyleSheet, Text, View } from "react-native";
import React from "react";
const FixedIncomeScreen = (props) => {
  return (
    <View>
      <Text> Wpływy stałe</Text>
    </View>
  );
};

export default FixedIncomeScreen;

const styles = StyleSheet.create({});
